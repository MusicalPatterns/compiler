// tslint:disable no-magic-numbers

import { Ms, Ordinal, to } from '@musical-patterns/utilities'

const NOT_FOUND: Ordinal = to.Ordinal(-1)
const HAS_NO_REPETEND: Ms = to.Ms(-1)

export {
    HAS_NO_REPETEND,
    NOT_FOUND,
}
